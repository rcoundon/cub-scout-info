# Use hoisted node_modules (faster, less disk space)
node-linker=hoisted

# Auto install peers
auto-install-peers=true

# Strict peer dependencies
strict-peer-dependencies=false

# Save exact versions
save-exact=true

# Engine strict (require correct Node version)
engine-strict=true
